
const keepImages = async(req , res) => {
    try{
        const { } = req.body;

    }catch(error) {
        return res.json({
            success: false,
            message: "failed to keep images with notes"
        }) 
    }
}